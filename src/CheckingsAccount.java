
public class CheckingsAccount extends Account{
    public CheckingsAccount(int account_id, int user_id, String currency, double balance, AccountType type) {
        super(account_id, user_id, currency, balance, type);
    }
}
